import { TestBed } from '@angular/core/testing';
import { TasksQuery } from './tasks.query';

describe('TasksQuery:', () => {
  let tasksQuery: TasksQuery;

  beforeEach(() => {
    tasksQuery = TestBed.get(TasksQuery);
  });
});
