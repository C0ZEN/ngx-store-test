import { ErrorStateMatcherProvider } from './error-state-matcher.provider';

describe('ErrorStateMatcherProvider:', () => {
  it('should be set', () => {
    expect(ErrorStateMatcherProvider).toBeDefined();
  });
});
